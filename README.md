# DevPilot

DevPilot 是一个面向多 IDE / 多 AI Agent 场景的统一监控面板。

当前仓库处于 **bootstrap** 阶段；在 Task 2 完成之前，**不保证** `npm run dev` / `npm run build` / `npm run preview` 能够正常运行（`package.json` 中的脚本仅为后续集成预留）。

## MVP

- 实时监控 Cursor / CodeBuddy / PyCharm(CodeBuddy 插件) 会话
- 展示状态、任务摘要、运行时长
- 支持审批与结构化选择
